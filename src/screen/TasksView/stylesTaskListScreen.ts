// stylesTaskListScreen.ts

import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    toggleButton: {
        backgroundColor: '#4caf50',
        padding: 10,
        margin: 10,
        borderRadius: 5,
        alignItems: 'center',
    },
    buttonText: {
        color: 'white',
        fontWeight: 'bold',
    },
});
